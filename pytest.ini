[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short

# HTML report settings (use with: pytest --html=reports/report.html)
# To generate HTML report: pytest --html=reports/report.html --self-contained-html
